version: "3.11"

services:

  flask:
    build: ./flask
    expose:
      - 8080
    environment:
      - TZ=Asia/Taipei

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - flask